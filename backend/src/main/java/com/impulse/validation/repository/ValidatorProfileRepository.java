package com.impulse.validation.repository;

import com.impulse.validation.ValidatorProfile;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ValidatorProfileRepository extends JpaRepository<ValidatorProfile, String> {
}
