package com.impulse.validation.repository;

import com.impulse.validation.ValidatorPool;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ValidatorPoolRepository extends JpaRepository<ValidatorPool, Long> {
}
