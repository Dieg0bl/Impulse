package com.impulse.validation.repository;

import com.impulse.validation.RedistributionRule;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RedistributionRuleRepository extends JpaRepository<RedistributionRule, Long> {
}
