package com.impulse.validation.repository;

import com.impulse.validation.ValidationSLA;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ValidationSLARepository extends JpaRepository<ValidationSLA, String> {
}
