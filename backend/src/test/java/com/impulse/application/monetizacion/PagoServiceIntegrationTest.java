package com.impulse.application.monetizacion;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

// Pequeña prueba de integración que arranca el contexto mínimo (usa H2 en runtime si está configurado)
@SpringBootTest
class PagoServiceIntegrationTest {

    @Test
    void contextLoads() {
        // Si el contexto Spring levanta, la app esencial está integrada
    }
}
