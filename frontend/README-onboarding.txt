# Onboarding rápido para desarrolladores

1. Clona el repositorio y entra en la carpeta:
   git clone <repo-url>
   cd Impulse/frontend
2. Instala dependencias:
   npm install
3. Para desarrollo local:
   npm start
4. Para ejecutar tests:
   npx jest
5. Para ver cobertura:
   npx jest --coverage
6. Para analizar ciclos:
   npx madge --circular src/

Revisa el archivo .github/pull_request_template.md antes de abrir un PR.
