import React from 'react';
const EvidenceUpload: React.FC<{ challengeId: string }> = ({ challengeId }) => <div>Subir evidencia para reto {challengeId} (placeholder)</div>;
export default EvidenceUpload;
